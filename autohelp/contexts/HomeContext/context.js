import { createContext } from 'react'

import {
    HEADER,
    MAIN_SECTION,
    WHY_US,
    OUR_SERVICES,
    LOAD_CAPACITY,
    WHERE_WE_WORK,
    TIRE_SERVICES,
    OUR_WORK,
    CONTACTS
} from './constants'

export const HOME_CONSTANT = {
    HEADER,
    MAIN_SECTION,
    WHY_US,
    OUR_SERVICES,
    LOAD_CAPACITY,
    WHERE_WE_WORK,
    TIRE_SERVICES,
    OUR_WORK,
    CONTACTS
}

export const HomeContext = createContext()